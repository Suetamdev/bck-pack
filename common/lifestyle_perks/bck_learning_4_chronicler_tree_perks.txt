bloodlines_in_ink_perk = {
    lifestyle = learning_lifestyle
    tree = chronicler
    position = { 1 0 }
    icon = node_learning

    character_modifier = {
        monthly_dynasty_prestige = 0.25
    }

    auto_selection_weight = {
        value = 11
        if = {
            limit = {
                has_education_learning_trigger = yes
            }
            add = 1989
        }
        if = {
            limit = {
                OR = {
                    has_trait = eccentric
                    has_trait = diligent
                }
            }
            multiply = 5
        }
        if = {
            limit = {
                has_trait = patient
            }
            multiply = 10
        }
        if = {
            limit = {
                has_trait = impatient
            }
            multiply = 0
        }
        if = {
            limit = {
                can_start_new_lifestyle_tree_trigger = no
            }
            multiply = 0
        }
    }
}

insightful_annotations_perk = {
    lifestyle = learning_lifestyle
    tree = chronicler
    position = { 1 1 }
    icon = node_learning

    parent = bloodlines_in_ink_perk

    effect = {
        custom_tooltip = insightful_annotations_perk_description
    }
}

reflections_on_diplomacy_perk = {
    lifestyle = learning_lifestyle
    tree = chronicler
    position = { 0 2 }
    icon = node_learning

    parent = insightful_annotations_perk

    character_modifier = {
        monthly_prestige_gain_mult = 0.1
    }

    effect = {
        custom_tooltip = reflections_on_diplomacy_perk_description
    }
}

commanders_duty_perk = {
    lifestyle = learning_lifestyle
    tree = chronicler
    position = { 2 2 }
    icon = node_learning

    parent = insightful_annotations_perk

    character_modifier = {
        advantage = 3
    }

    effect = {
        custom_tooltip = commanders_duty_perk_description
    }
}

master_of_coin_perk = {
    lifestyle = learning_lifestyle
    tree = chronicler
    position = { 0 3 }
    icon = node_learning

    parent = reflections_on_diplomacy_perk

    government_character_modifier = {
		flag = government_is_landless_adventurer
        domicile_monthly_gold_add = 0.25
	}

	government_character_modifier = {
		flag = government_is_landless_adventurer
		invert_check = yes
		domain_tax_mult = 0.1
	}

    effect = {
        custom_tooltip = master_of_coin_perk_description
    }
}

shade_network_perk = {
    lifestyle = learning_lifestyle
    tree = chronicler
    position = { 2 3 }
    icon = node_learning

    parent = commanders_duty_perk

    character_modifier = {
        owned_scheme_secrecy_add = 5
    }

    effect = {
        custom_tooltip = shade_network_perk_description
    }
}

herbs_and_anatomy_perk = {
    lifestyle = learning_lifestyle
    tree = chronicler
    position = { 0 4 }
    icon = node_learning

    parent = master_of_coin_perk

    character_modifier = {
        negate_health_penalty_add = 0.5
    }

    effect = {
        custom_tooltip = herbs_and_anatomy_perk_description
    }
}

tales_of_old_perk = {
    lifestyle = learning_lifestyle
    tree = chronicler
    position = { 2 4 }
    icon = node_learning

    parent = shade_network_perk

    character_modifier = {
        character_travel_safety_mult = 0.15
    }

    effect = {
        custom_tooltip = tales_of_old_perk_description
    }
}

chronicler_perk = {
	lifestyle = learning_lifestyle
	tree = chronicler
	position = { 1 5 }
	icon = trait_chronicler

	parent = herbs_and_anatomy_perk
	parent = tales_of_old_perk

	trait = chronicler

	effect = {
		add_trait_force_tooltip = chronicler
	}
}